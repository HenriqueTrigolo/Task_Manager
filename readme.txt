Para iniciar a página, executar o comando 'npm install' para que as dependências sejam baixadas. Depois, executar o comando 'npm start' para que o servidor acesse a porta 3000.
Para vizualização, acessar o localhost:3000 

(Apache e MySQL devm estar ligados)